= Squeezy

CSS and JS go in, compressed version go out, that's what Squeezy is all about.

== Prerequisites

Must be run under jRuby.

== Usage

* <tt>gem install squeezy</tt>
* <tt>squeezy</tt>
* <tt>curl -T myjavascript.js localhost:4567/js</tt>

